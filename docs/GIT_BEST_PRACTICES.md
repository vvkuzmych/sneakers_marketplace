# 📘 Git Best Practices

## 🚨 Важливо: Переміщення та перейменування файлів

### ❌ НЕПРАВИЛЬНО

```bash
# НЕ використовуйте звичайний mv
mv file.md docs/file.md
git add .
git commit -m "Move file"
```

**Проблеми:**
- ❌ Git бачить це як **delete + create new file**
- ❌ Втрачається вся Git history файлу
- ❌ `git blame` не працює правильно
- ❌ `git log` не показує історію до переміщення
- ❌ Важко знайти хто і коли робив зміни

---

### ✅ ПРАВИЛЬНО

```bash
# Використовуйте git mv
git mv file.md docs/file.md
git commit -m "docs: move file.md to docs/ directory"
```

**Переваги:**
- ✅ Git розуміє що це **rename/move**
- ✅ Зберігається повна Git history
- ✅ `git blame` показує правильних авторів
- ✅ `git log --follow` відстежує файл до переміщення
- ✅ Легко знайти всю історію змін

---

## 📋 Приклади

### Перемістити один файл

```bash
git mv README.md docs/README.md
```

### Перемістити декілька файлів

```bash
git mv file1.md file2.md file3.md docs/
```

### Перейменувати файл

```bash
git mv old-name.md new-name.md
```

### Перемістити і перейменувати

```bash
git mv src/old-name.md docs/new-name.md
```

---

## 🔍 Перевірка історії після переміщення

### Подивитись історію файлу

```bash
# --follow каже Git відстежувати файл через rename/move
git log --follow docs/file.md
```

### Подивитись хто змінював кожен рядок

```bash
git blame docs/file.md
```

### Подивитись статистику змін з rename

```bash
# -M виявляє переміщення файлів
git log --stat -M docs/file.md
```

### Подивитись diff з rename detection

```bash
git log -p -M --follow docs/file.md
```

---

## 🛠️ Виправлення помилки (якщо вже використали mv)

Якщо ви вже використали `mv` і втратили Git history:

```bash
# 1. Відмінити зміни (якщо ще не закомітили)
git reset HEAD .
git checkout -- old-file.md

# 2. Видалити неправильно створені файли
rm docs/old-file.md

# 3. Використати правильну команду
git mv old-file.md docs/old-file.md

# 4. Закомітити
git commit -m "docs: move old-file.md to docs/"
```

---

## 📦 Масове переміщення файлів

### Bash script для переміщення багатьох файлів

```bash
#!/bin/bash
# move-to-docs.sh

FILES=(
    "FILE1.md"
    "FILE2.md"
    "FILE3.md"
)

for file in "${FILES[@]}"; do
    if [ -f "$file" ]; then
        git mv "$file" docs/
        echo "✅ Moved: $file"
    fi
done
```

**Використання:**

```bash
chmod +x move-to-docs.sh
./move-to-docs.sh
git commit -m "docs: organize documentation into docs/ directory"
```

---

## 🎯 Правила для проекту

### 1. **Завжди використовуйте `git mv`**

```bash
# ✅ Правильно
git mv source.md destination.md

# ❌ Неправильно
mv source.md destination.md
git add .
```

### 2. **Перевіряйте Git status**

```bash
git status

# Шукайте "R" (rename) замість "D" + "A"
# ✅ Правильно:  R  old.md -> new.md
# ❌ Неправильно: D  old.md
#                A  new.md
```

### 3. **Використовуйте описові commit messages**

```bash
# ✅ Добре
git commit -m "docs: move API documentation to docs/ directory"
git commit -m "refactor: rename UserService to UserHandler"

# ❌ Погано
git commit -m "move files"
git commit -m "rename"
```

### 4. **Перевіряйте історію після commit**

```bash
# Перевірте що Git зберіг історію
git log --follow --oneline docs/new-file.md
```

---

## 🚀 Quick Reference

| Дія | Команда |
|-----|---------|
| Перемістити файл | `git mv file.md docs/` |
| Перейменувати файл | `git mv old.md new.md` |
| Перемістити + перейменувати | `git mv old.md docs/new.md` |
| Перемістити кілька | `git mv file1.md file2.md docs/` |
| Подивитись історію | `git log --follow file.md` |
| Blame з історією | `git blame file.md` |
| Показати rename в log | `git log --stat -M` |

---

## 💡 Чому це важливо?

1. **Зберігає контекст** - можна знайти хто писав код і чому
2. **Полегшує code review** - зрозуміло що файл просто перемістили
3. **Допомагає в debugging** - легко знайти коли з'явився баг
4. **Покращує collaboration** - команда бачить повну історію
5. **Git blame працює** - знаєш кого питати про код

---

## 📚 Корисні посилання

- [Git Documentation: git-mv](https://git-scm.com/docs/git-mv)
- [Git Book: Moving Files](https://git-scm.com/book/en/v2/Git-Basics-Recording-Changes-to-the-Repository#_moving_files)
- [Git Log --follow](https://git-scm.com/docs/git-log#Documentation/git-log.txt---follow)

---

**📌 Пам'ятайте: `git mv` - ваш друг! Завжди використовуйте його для переміщення та перейменування файлів в Git репозиторії.**

---

*Останнє оновлення: 2026-01-23*
